"use client";
import Link from "next/link";
import { useCart } from "./CartProvider";
export function CartSummary({compact=false}:{compact?:boolean}){const {subtotal,tax,total,count}=useCart();return <aside className="border border-ink/15 bg-ivory p-5"><div className="flex items-center justify-between"><p className="font-serif text-xl">Your order</p><span className="text-xs text-muted">{count} items</span></div><div className="mt-6 space-y-3 border-y border-ink/10 py-4 text-sm"><p className="flex justify-between"><span>Subtotal</span><span>${subtotal.toFixed(2)}</span></p><p className="flex justify-between text-muted"><span>Estimated tax</span><span>${tax.toFixed(2)}</span></p></div><p className="mt-4 flex justify-between font-serif text-xl"><span>Total</span><span>${total.toFixed(2)}</span></p>{!compact&&<Link href="/checkout" className="mt-6 block bg-ink px-5 py-3 text-center text-[11px] font-bold uppercase tracking-[.12em] text-white">Checkout demo</Link>}<Link href="/cart" className="mt-3 block text-center text-[10px] font-bold uppercase tracking-[.14em] underline underline-offset-4">View bag</Link></aside>}
